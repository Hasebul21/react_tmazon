import React from 'react';
import { Outlet } from 'react-router-dom';

const Event = () => {
    return (
        <div>
            <h1>Event Component</h1>
        </div>
    );
};

export default Event;